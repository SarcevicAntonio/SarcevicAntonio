# dotfiles

not really dotfiles but whatever

## vscode extensions

```bash
# from dotfiles folder
cd dotfiles
# list extensions:
codium --list-extensions > vscode.list-extensions
# install:
./install-extensions.sh
```
